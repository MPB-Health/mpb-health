# Supabase (required for website — reads content/blog/plans from legacy project for now)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# ARYX CRM lead intake (REQUIRED in Production as of the MPB → ARYX
# single-CRM consolidation cutover, 2026-05-28). Get-a-Quote leads POST
# directly to https://${ARYX_PROJECT}.supabase.co/functions/v1/crm-website-lead-intake
# instead of going through `supabase.functions.invoke` on the legacy project.
VITE_ARYX_FUNCTIONS_URL=https://knelbprqqbjggqfqvfmc.supabase.co
VITE_ARYX_ANON_KEY=

# Resend - Email sending (domain: mpb.health)
# Get your API key from https://resend.com/api-keys
# VITE_RESEND_API_KEY is intentionally absent — set RESEND_API_KEY in Supabase Edge Function secrets instead.
